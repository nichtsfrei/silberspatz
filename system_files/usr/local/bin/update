#!/bin/sh

# take a look at topgrade
rpm-ostree update

command -v flatpak > /dev/null 2>&1 && flatpak update -y
command -v distrobox > /dev/null 2>&1 && distrobox upgrade -a

#rpm-ostree db diff --changelogs

# fwupdmgr refresh --force
# fwupdmgr get-updates
# fwupdmgr update
