#!/bin/sh

set -e 
[ -z "$1" ] && exit 1 || SERVICE="$1"
[ -z "$2" ] && FILE="/tmp/$SERVICE.plain" && uuidgen > "$FILE" || FILE="$2"
TARGET="/etc/systemd/credentials/$SERVICE.cred"

systemd-creds encrypt $FILE $TARGET
printf "Created $TARGET"
